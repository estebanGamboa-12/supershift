# Copia este archivo a `.env.local` y ajusta los valores según tu proyecto Supabase
SUPABASE_URL=https://mdnwgylgkvolvqimjfxu.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
DEFAULT_CALENDAR_ID=2

# URLs públicas (producción con subdominios)
# planloop.app = landing; app.planloop.app = app
NEXT_PUBLIC_APP_URL=https://app.planloop.app

# En local: NEXT_PUBLIC_SITE=landing para ver la landing en localhost (por defecto se ve la app)
# NEXT_PUBLIC_SITE=landing

# Configuración pública de Supabase (interfaz web)
NEXT_PUBLIC_SUPABASE_URL=https://mdnwgylgkvolvqimjfxu.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1kbndneWxna3ZvbHZxaW1qZnh1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTk4NjY3NjQsImV4cCI6MjA3NTQ0Mjc2NH0.OQFN0kXAYHkV-HMXNJNXPAkFpeXii5DvDwXy5MXPuuY

# Supabase → Authentication → URL Configuration → Redirect URLs
# Añade SOLO estas dos URLs (copia tal cual, sin barra final):
#   https://app.planloop.app/auth/callback
#   http://localhost:3000/auth/callback
# Site URL en producción: https://app.planloop.app
#
# Para registro con correo (verificación por email):
# Authentication → Providers → Email: "Confirm email" activado.
# Authentication → Settings: "Enable new user signups" activado.
# El enlace de confirmación redirige a /auth/callback.

# Correo (Brevo o Resend): verificación, recuperación, bienvenida y "sesión iniciada"
# BREVO_API_KEY=       # API Key en Brevo → Claves API y MCP
# EMAIL_FROM=Planloop <noreply@planloop.app>
# Añade ese remitente en Brevo → Remitentes y verifícalo. Luego pon aquí el mismo (nombre y correo).

# Notificaciones push (opcional): genera un par con `npx web-push generate-vapid-keys`
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=

